Webhook delivery logs are retained for 30 days. Each failed delivery is retried up to three times before it is marked as failed.

Webhook payloads are signed with an HMAC SHA-256 signature. Verify the signature before processing an event.
